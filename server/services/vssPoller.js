// VSS (NVIDIA Video Search and Summarization) poller — placeholder only.
// Configure VSS_BASE_URL, VSS_API_KEY, and camera IDs in server/config/vss.js
// before enabling actual polling.

function start() {
    // No-op until VSS deployment details are configured.
}

function stop() {}

module.exports = { start, stop };
